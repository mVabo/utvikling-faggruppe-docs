@page "/"

@inject NavigationManager Navigator

<PageTitle>Index</PageTitle>

<h1>Hello, world!</h1>

Loading..

@code {
  // A basic function that redirects us to the correc
  // url when the app is ready
  protected override async Task OnInitializedAsync()
  {
    Navigator.NavigateTo("/initialize");
  }
}